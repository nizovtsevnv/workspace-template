# Workspace Template

Шаблон рабочего пространства для разработки модульных проектов с хэлперами для C, Zig, Node.js, PHP, Python, Rust.

## 🚀 Быстрый старт

### 1. Создать проект из шаблона

```bash
# Клонировать шаблон
git clone https://github.com/nizovtsevnv/workspace-template.git my-project
cd my-project
make init
```

**Готово!** Теперь можно создавать модули проекта.

### 2. Создать модули проекта

```bash
# Создать новый модуль (интерактивно)
make modules create
# → Выбор стека (C, Zig, Node.js, PHP, Python, Rust)
# → Выбор типа проекта (bun, npm, composer, poetry, cargo, etc.)
# → Ввод имени модуля
# → Автоматическая инициализация проекта

# Или с параметрами (для автоматизации)
make modules create MODULE_STACK=nodejs MODULE_TYPE=bun MODULE_NAME=my-service
make modules create MODULE_STACK=python MODULE_TYPE=poetry MODULE_NAME=my-lib
make modules create MODULE_STACK=rust MODULE_TYPE=bin MODULE_NAME=my-tool
```

**Поддерживаемые типы модулей:**
- **C**: makefile, cmake
- **Zig**: exe (приложение), lib (библиотека)
- **Node.js**: bun, npm, pnpm, yarn, Next.js, Expo, SvelteKit
- **PHP**: composer-lib, composer-project, Laravel
- **Python**: uv, poetry
- **Rust**: bin (приложение), lib (библиотека), Dioxus (веб-приложение)

### 3. Разработка

Используйте ваши любимые инструменты для разработки:
- IDE/редактор на хосте (VS Code, vim, IntelliJ IDEA)
- Инструменты стеков установленные локально
- Makefile для автоматизации задач

### 4. Публикация проекта

```bash
# Настроить удаленный репозиторий для вашего проекта
git remote add origin git@github.com:your-org/your-project.git

# Отправить в ваш репозиторий
git push -u origin main
```

---

## 📦 Архитектура

### Децентрализованные генераторы модулей

Система генерации модулей основана на специализированных Alpine контейнерах вместо монолитного devcontainer.

**Преимущества:**
- 🚀 Не требует установки среды разработки для генерации
- 📦 Маленькие образы (~50-500MB вместо 2GB monolithic devcontainer)
- ⚡ Быстрая инициализация для CI/CD
- 🔄 Изолированные окружения для каждого стека
- 🎯 Легко расширять (добавить стек = Dockerfile + генератор)

**Размеры образов:**

| Стек    | Размер образа | Инструменты                    |
|---------|---------------|--------------------------------|
| C       | ~50 MB        | gcc, make, cmake, clang-format |
| Zig     | ~462 MB       | zig, llvm, clang               |
| Node.js | ~150 MB       | node, npm, bun, pnpm           |
| PHP     | ~100 MB       | php, composer, laravel         |
| Python  | ~100 MB       | python, uv, poetry             |
| Rust    | ~500 MB       | rustc, cargo, rustfmt, clippy  |

Подробнее: [doc/development/template/generators.md](doc/development/template/generators.md)

---

## 📂 Структура проекта

```
my-project/
├── .template/              # Генераторы модулей и ассеты
│   ├── dockerfiles/        # Dockerfiles генераторов (nodejs, python, rust, c, zig, php)
│   ├── scripts/            # Скрипты генераторов
│   └── assets/             # Конфигурации стандартов качества
├── .github/workflows/      # GitHub Actions
├── doc/                    # Документация проекта
├── makefiles/              # Модули системы автоматизации
├── modules/                # Git субмодули (ваши компоненты)
└── Makefile                # Главный makefile
```

---

## 📚 Документация

- [doc/development/template/generators.md](doc/development/template/generators.md) - Децентрализованные генераторы модулей
- [doc/development/template/makefile.md](doc/development/template/makefile.md) - Система автоматизации Makefile
- [doc/development/template/file-tree.md](doc/development/template/file-tree.md) - Описание файловой структуры

---

## 🔧 Кастомизация

### Добавить новый стек

Для добавления нового стека:

1. Создать `.template/dockerfiles/<stack>.Dockerfile`
2. Создать `.template/scripts/modules/generate-<stack>.sh`
3. Добавить конфигурации в `.template/assets/<stack>/`
4. Обновить wizard в `module-create.sh`

Подробнее: [doc/development/template/generators.md](doc/development/template/generators.md)

---

## ⚙️ Системные требования

- **Docker** или **Podman** (для генераторов модулей)
- **Git** >= 2.40
- **Make** >= 4.0 (опционально, для автоматизации)

---

## 🐳 Docker vs Podman

Генераторы модулей работают с обоими container runtime:

**Docker:**
```bash
# Linux
sudo apt-get install docker.io

# macOS / Windows
# Установите Docker Desktop
```

**Podman:**
```bash
# Linux
sudo apt-get install podman
systemctl --user enable --now podman.socket
```

---

## 💡 Типичный workflow разработчика

1. **Скачать шаблон** → клонировать репозиторий
2. **Создать модули** → `make modules create` для каждого компонента проекта
3. **Разработка** → код, документация, автоматизация (на хосте)
4. **Коммиты** → работа с Git как обычно
5. **Push** → отправить в ваш репозиторий проекта

---

## 🔄 Обновление среды

### Обновление шаблона Workspace Template

Проекты, инициализированные через `make init`, могут получать обновления из репозитория шаблона:

```bash
# Проверить статус и доступные обновления
make template status
# Показывает:
# → URL репозитория шаблона
# → Текущую версию шаблона (hash + дата)
# → Последнюю доступную версию
# → Доступность обновлений

# Применить обновления из шаблона
make template update
# Интерактивный процесс:
# → Fetch обновлений из репозитория шаблона
# → Просмотр списка изменений между версиями
# → Подтверждение обновления
# → Автоматический merge с обработкой конфликтов
# → Создание коммита обновления
```

**Переопределение URL шаблона:**

URL репозитория шаблона по умолчанию: `https://github.com/nizovtsevnv/workspace-template.git`

Его можно переопределить через переменную окружения:

```bash
# Использовать другой репозиторий шаблона
TEMPLATE_REPO_URL=https://your-template.git make template update

# Или экспортировать глобально
export TEMPLATE_REPO_URL=https://your-template.git
make template status
make template update
```

---

## 🐛 Устранение проблем

### Образы генераторов не собираются

```bash
# Проверить docker/podman
docker --version
# или
podman --version

# Пересобрать образ вручную
podman build -t devcontainer-nodejs -f .template/dockerfiles/nodejs.Dockerfile .template/dockerfiles/

# Или удалить и пересобрать автоматически
podman rmi devcontainer-nodejs
make modules create MODULE_STACK=nodejs MODULE_TYPE=bun MODULE_NAME=test
```

### Проблемы с правами доступа

Генераторы используют `HOST_UID` и `HOST_GID` для создания файлов с правильными правами. Если файлы создаются с root правами - проверьте переменные окружения.

### Нет прав на Docker socket

```bash
# Linux - добавить пользователя в группу docker
sudo usermod -aG docker $USER
# Перелогиньтесь

# Или используйте Podman
```

---

## 📋 Примеры использования

### Система автоматизации Make

Шаблон включает простую систему автоматизации:

```bash
# Команды шаблона
make template init           # Ручная инициализация (если нужно)
make template update         # Обновить версию шаблона
make template status         # Текущий статус и версия шаблона
make template test           # Тесты генераторов

# Создание модулей
make modules                # Создать новый модуль (интерактивно)
make modules create MODULE_STACK=nodejs MODULE_TYPE=bun MODULE_NAME=my-service

# Команды модулей
make <модуль>              # Справка по модулю

# Справка
make help                  # Все команды
```

Подробная документация: [doc/development/template/makefile.md](doc/development/template/makefile.md)

### Работа с субмодулями

```bash
# Инициализация и обновление всех субмодулей
make modules pull

# Проверить статус всех субмодулей
make modules status

# Отправить изменения во все субмодули
make modules push

# Работа с отдельным субмодулем
cd modules/my-service
git checkout develop

# Или через команду шаблона для отдельного модуля
make my-service pull    # Обновить один модуль
make my-service push    # Отправить изменения одного модуля
```

---

## 🤝 Вклад в шаблон

Если вы хотите улучшить сам шаблон:

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения
4. Создайте Pull Request

---

## 📄 Лицензия

Этот шаблон распространяется свободно. Используйте в своих проектах без ограничений.

---

## 🔗 Ссылки

- [Репозиторий шаблона](https://github.com/nizovtsevnv/devcontainer-workspace)
- [Git Submodules](https://git-scm.com/book/en/v2/Git-Tools-Submodules)
- [Alpine Linux](https://alpinelinux.org/)
