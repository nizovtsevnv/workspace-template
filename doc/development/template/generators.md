# Децентрализованные генераторы модулей

Система генерации модулей основана на специализированных Alpine контейнерах вместо централизованного devcontainer.

## Архитектура

### Принцип работы

1. **Специализированные образы**: Каждый стек имеет свой легковесный Alpine образ (~50-500MB)
2. **Автономные генераторы**: Генераторы работают независимо от devcontainer
3. **Автоматический fallback**: Образы собираются автоматически при первом использовании
4. **Git-инициализация на хосте**: После генерации модуля выполняется `git init` на хосте

### Структура файлов

```
.template/
├── dockerfiles/           # Dockerfiles для генераторов
│   ├── nodejs.Dockerfile  # Node.js (npm, bun, pnpm, yarn)
│   ├── python.Dockerfile  # Python (uv, poetry)
│   ├── rust.Dockerfile    # Rust (cargo, rustfmt, clippy)
│   ├── c.Dockerfile       # C (gcc, make, cmake, clang-format)
│   ├── zig.Dockerfile     # Zig
│   └── php.Dockerfile     # PHP (composer, laravel)
├── scripts/
│   ├── lib/
│   │   └── generator.sh   # Библиотека запуска генераторов
│   └── modules/           # Скрипты генераторов
│       ├── generate-nodejs.sh
│       ├── generate-python.sh
│       ├── generate-rust.sh
│       ├── generate-c.sh
│       ├── generate-zig.sh
│       └── generate-php.sh
└── assets/                # Конфигурации и шаблоны (с декомпозицией)
    ├── README.md          # Общий шаблон README
    ├── nodejs/
    │   ├── common/        # Общие конфиги (.eslintrc, .prettierrc, tsconfig.json)
    │   ├── base/          # README и .gitignore для простых проектов
    │   ├── nextjs/        # README для Next.js
    │   ├── expo/          # README для Expo
    │   ├── svelte/        # README для SvelteKit
    │   └── supabase/      # README и .gitignore для Supabase
    ├── python/
    │   ├── common/        # .flake8, ruff.toml, .pylintrc
    │   ├── uv/            # README для uv
    │   └── poetry/        # README для poetry
    ├── rust/
    │   ├── common/        # rustfmt.toml, clippy.toml
    │   ├── bin/           # README для binary
    │   ├── lib/           # README для library
    │   └── dioxus/        # README для dioxus
    ├── c/
    │   ├── common/        # .clang-format, .editorconfig
    │   ├── makefile/      # README для makefile
    │   └── cmake/         # README для cmake
    ├── php/
    │   ├── common/        # .php-cs-fixer.php, phpstan.neon
    │   ├── composer-lib/  # README для библиотеки
    │   ├── composer-project/ # README для проекта
    │   └── laravel/       # README для Laravel
    └── zig/
        ├── common/        # (пусто, для единообразия)
        ├── exe/           # README для executable
        └── lib/           # README для library
```

### Декомпозиция assets

Для сложных генераторов (Node.js, PHP, Rust) структура assets разделена на:

- **common/** - общие конфигурации, копируемые для всех типов модулей (линтеры, форматтеры)
- **type-specific/** - специфичные файлы для каждого типа (README, .gitignore, дополнительные конфиги)

При создании модуля сначала копируются файлы из `common/`, затем из типо-специфичной директории. Это позволяет:

- ✅ Избежать дублирования общих конфигураций
- ✅ Легко добавлять новые типы модулей
- ✅ Переопределять файлы из common в type-specific (например, разные .gitignore)
- ✅ Поддерживать единообразие для всех стеков

Пример использования в генераторе:
```sh
copy_stack_assets "nodejs" "$MODULE_TARGET/$MODULE_NAME" "$MODULE_TYPE"
# Копирует: nodejs/common/* + nodejs/supabase/*
```

## Использование

### Создание модуля

```bash
# Через интерактивный мастер
make module create

# Или напрямую
make module create MODULE_STACK=nodejs MODULE_TYPE=bun MODULE_NAME=my-app

# В произвольную директорию
make module create MODULE_STACK=rust MODULE_TYPE=bin MODULE_NAME=my-tool MODULE_TARGET=/custom/path
```

### Поддерживаемые стеки и типы

**C**:
- `makefile` - классический проект с Makefile
- `cmake` - современный проект с CMake

**Node.js**:
- `bun`, `npm`, `pnpm`, `yarn` - простые проекты
- `nextjs` - Next.js с TypeScript и Tailwind
- `expo` - React Native с Expo
- `svelte` - SvelteKit
- `supabase` - Backend с Supabase (миграции, Edge Functions, типы)

**PHP**:
- `composer-lib` - библиотека Composer
- `composer-project` - проект Composer
- `laravel` - Laravel приложение

**Python**:
- `uv` - быстрый менеджер пакетов (рекомендуется)
- `poetry` - классический Poetry

**Rust**:
- `bin` - исполняемое приложение
- `lib` - библиотека
- `dioxus` - веб-приложение с Dioxus

**Zig**:
- `exe` - исполняемое приложение
- `lib` - библиотека

## Размеры образов

| Стек    | Размер образа | Инструменты                    |
|---------|---------------|--------------------------------|
| C       | ~50 MB        | gcc, make, cmake, clang-format |
| Zig     | ~462 MB       | zig, llvm, clang               |
| Node.js | ~150 MB       | node, npm, bun, pnpm           |
| Python  | ~100 MB       | python, uv, poetry             |
| Rust    | ~500 MB       | rustc, cargo, rustfmt, clippy  |
| PHP     | ~100 MB       | php, composer, laravel         |

Сравнение с devcontainer: **~2 GB** → **50-500 MB per stack**

## Автоматизация

### Сборка образов

Образы собираются автоматически при первом использовании генератора:

```bash
# Первый запуск - автоматическая сборка
make module create MODULE_STACK=nodejs MODULE_TYPE=bun MODULE_NAME=my-app
# 🔨 Сборка образа devcontainer-nodejs...
# ✅ Образ собран успешно

# Последующие запуски используют кеш
make module create MODULE_STACK=nodejs MODULE_TYPE=npm MODULE_NAME=another-app
# (без сборки, использует существующий образ)
```

### Очистка образов

```bash
# Удалить все образы генераторов
podman rmi devcontainer-{nodejs,python,rust,c,zig,php}

# Или через функцию библиотеки
. .template/scripts/lib/generator.sh
cleanup_generator_images
```

## Интерактивные генераторы

Для некоторых типов модулей (Next.js, Expo, SvelteKit) используется интерактивный режим:

```bash
make module create MODULE_STACK=nodejs MODULE_TYPE=nextjs MODULE_NAME=my-app
# Запустится интерактивный wizard создания Next.js приложения
```

Поддерживается через `run_generator_interactive()` с флагом `-it` в docker/podman.

## Git-интеграция

После создания модуля автоматически выполняется:

```bash
cd /path/to/module
git init
git add .
git commit -m "Initial commit from template"
```

Если git недоступен на хосте - пропускается с предупреждением.

## Копирование конфигураций

При создании модуля автоматически копируются:

1. **README.md** из `.template/assets/README.md` с подстановками:
   - `{{MODULE_NAME}}` → имя модуля
   - `{{STACK}}` → название стека
   - `{{TYPE}}` → тип проекта

2. **Конфигурации стека** из `.template/assets/<stack>/`:
   - Форматтеры кода (prettier, rustfmt, clang-format)
   - Линтеры (eslint, flake8, clippy)
   - Настройки редактора (.editorconfig)

## Монтирование директорий

Система автоматически обрабатывает монтирование:

- **Внутри workspace**: монтируется только workspace root
- **Вне workspace** (например, `/tmp`): автоматическое дополнительное монтирование целевой директории

Пример:
```bash
# Внутри workspace
make module create MODULE_NAME=app MODULE_TARGET=modules
# -v /workspace:/workspace

# Вне workspace
make module create MODULE_NAME=app MODULE_TARGET=/tmp/test
# -v /workspace:/workspace -v /tmp/test:/tmp/test
```

## Преимущества

- 🚀 **Не требует devcontainer** для генерации модулей
- 📦 **Маленькие образы** (в 4-40 раз меньше devcontainer)
- ⚡ **Быстрая инициализация** для CI/CD
- 🔄 **Изолированные окружения** для каждого стека
- 🎯 **Легко расширять** - добавить стек = Dockerfile + генератор
- 📝 **Единые стандарты** через конфигурации в assets/
- 🔧 **Автоматическое управление** образами и зависимостями

## Разработка новых генераторов

Для добавления нового стека:

1. Создать `.template/dockerfiles/<stack>.Dockerfile`
2. Создать `.template/scripts/modules/generate-<stack>.sh`
3. Добавить конфигурации в `.template/assets/<stack>/`
4. Обновить wizard в `module-create.sh`

Пример генератора:

```bash
#!/bin/sh
set -e

MODULE_TYPE="$1"
MODULE_NAME="$2"
MODULE_TARGET="$3"

case "$MODULE_TYPE" in
    type1)
        # Генерация type1
        ;;
    type2)
        # Генерация type2
        ;;
    *)
        echo "Неизвестный тип: $MODULE_TYPE" >&2
        exit 1
        ;;
esac

# Копирование конфигов из assets
if [ -d "/workspace/.template/assets/<stack>" ]; then
    cp -r /workspace/.template/assets/<stack>/. "$MODULE_TARGET/$MODULE_NAME/"
fi

echo "✅ Модуль создан: $MODULE_TARGET/$MODULE_NAME"
```

## Troubleshooting

### Ошибка сборки образа

```bash
# Пересобрать образ вручную
podman build -t devcontainer-nodejs -f .template/dockerfiles/nodejs.Dockerfile .template/dockerfiles/

# Или удалить и пересобрать автоматически
podman rmi devcontainer-nodejs
make module create MODULE_STACK=nodejs ...
```

### Проблемы с правами доступа

Генераторы используют `HOST_UID` и `HOST_GID` для создания файлов с правильными правами. Если файлы создаются с root правами - проверьте переменные окружения.

### Модуль создается не в той директории

Проверьте `MODULE_TARGET` - должен быть абсолютный или относительный путь. Система автоматически монтирует директории вне workspace.
